/**
 * Drizzle schema entry point.
 *
 * Phase 1 부트스트랩 단계에서는 Prisma 스키마가 여전히 단일 소스이며,
 * Drizzle 마이그레이션과 테이블 정의는 Phase 2에서 채워집니다.
 * 이 파일은 drizzle-kit 설정이 안정적으로 동작하도록 자리만 잡아둡니다.
 */

export const drizzleSchemaBootstrapReady = true;
